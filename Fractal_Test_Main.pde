/*
Trees tree;
boolean drawTree;

void setup() {
  size(1920, 1080);
  //frameRate (1);
  background (254,150,90);
  
  
  tree = new Trees ();
}

void draw() {
  //background(0);
  stroke(255);
  
  if (drawTree) {
    background (254,150,90);
    tree.drawTrees();
    drawTree = false;
    tree.i++;
  }
}

void mousePressed() { 
    drawTree = true; 
}
*/
